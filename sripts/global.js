// テトリミノのおおきさ
const tetroSize = 20;

// 行とか列の数
const rowMax = 20;
const colMax = 10;

// キャンバスの大きさ
const canvasW = tetroSize * colMax;
const canvasH = tetroSize * rowMax;

export { tetroSize, rowMax, colMax, canvasW, canvasH };
